# Deployment credentials for TL Manager (PoC / dev).
# Usernames and passwords in one place; plain English for easy reading.
# Loaded automatically for group [tlmanager]. Do not use in production as-is.
# MySQL user (created by mysql role) and app JDBC (context.xml + application.properties) must match.
mysql_tlmanager_user: tlmanager
tlmanager_db_user: tlmanager
mysql_tlmanager_password: "TrustedListManagerDatabasePassword"
tlmanager_db_password: "TrustedListManagerDatabasePassword"
